{
  "name": "solarpermitprepai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npx wrangler pages dev dist --compatibility-date=2026-05-22",
    "test": "npm run build && node --test tests/site.test.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name solarpermitprepai --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "4.113.0"
  }
}
